#매개 변수 탐색
#처음에 완전 탐색 생각했는데 10**4 * 10**4 시간 초과 날 듯 싶었음
# 최댓값 보고서 매개 변수 같다 생각했음
# 배열에서 최솟값 1, 맥스값 찾고서 mid 값 설정 (범위는 항상 어려워서 코드를 써봐야 함)
# for문 돌면서 mid 값과 배열 값을 비교해서 min값을 더하고
# for문 다 돌면 sum값이 예산보다 크다면 mid값을 줄이고 아니면 늘리고
